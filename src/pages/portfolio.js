import React from "react"
import Layout from "../components/layout"
import Projects from "../components/Projects"

export default ({ data }) => (
  <Layout>
    <h1>Portfolio</h1>
    <Projects />
  </Layout>
)
